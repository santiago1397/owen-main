"""Integrations with OTHER systems that are not telephony providers.

A sibling of `providers/` (carriers), `flows/` (call graphs) and `telephony/` (Asterisk
control). Everything under here is opt-in, kill-switched, and additive: with an integration
disabled the platform must be byte-for-byte the platform that existed before it.
"""
